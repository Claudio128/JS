var x = 0;

while(x < 5) {
    console.log("Testendo repetição" + x);

    // Incrementador
    x++;
}

var arr = ['teste', 'testando', 'a', 'b'];
var y = 0;

with(y <= 3){
    console.log(arr[y]);

    // Incrementador
    y++;
}

var palavra = "Claudio";
var i = 0;

while (i <= 6) {
    console.log(palavra[i]);

    // Incrementador
    i++;
}